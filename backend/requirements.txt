# --- web / api ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
python-dotenv>=1.0,<2.0

# --- persistence ---
SQLAlchemy>=2.0,<2.1

# --- verification engine (local NLI cross-encoder, no API calls) ---
# These exact versions produced the committed calibration artifact. The cache records the
# same versions and fails closed if they drift; changing one requires rebuilding it with
# `python eval/build_conformal_cache.py` and rerunning the held-out evaluation.
numpy==2.5.2
sentence-transformers==5.7.0
transformers==5.15.1
torch==2.13.0
tokenizers==0.22.2
sentencepiece==0.2.2
protobuf==7.36.0

# --- external services ---
# razorpay must be >=2.0: 1.x imports pkg_resources, which Python 3.12+ venvs no longer
# ship, so it cannot even be imported on a modern interpreter.
razorpay>=2.0,<3.0

# --- LLM drafting (optional at runtime; packets fall back to a deterministic template) ---
google-genai>=1.0
anthropic>=0.40

# --- tests ---
pytest>=8.3,<9.0
httpx>=0.27
