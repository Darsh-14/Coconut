[pytest]
pythonpath = .
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
markers =
    slow: tests that load the NLI model or run inference
    uses_llm: test stubs the LLM provider itself; opts out of the autouse disable
